<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BaseTemplate" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.StatusWhite" parent="Theme.BaseTemplate">
        <!-- This is used for status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- This is used for status bar content color. If status bar Color is light, use "true" otherwise use "false"-->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.StatusColorApp" parent="Theme.BaseTemplate">
        <item name="android:statusBarColor">@color/color_main</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>